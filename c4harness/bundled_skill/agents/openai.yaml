interface:
  display_name: "C4Harness"
  short_description: "Delegate coding work and monitor async workloads"
  default_prompt: "Use $c4harness to delegate this long task or monitor its workload asynchronously, then verify and return the result."
